#include<stdio.h>
int main () {
    
int base,height ;
float area ;
printf("Enter the base and height: ");
scanf(" %d %d", &base, &height) ;
area = 0.5 * base * height ;
printf("\n Area of triangle is %f", area) ;

    return 0 ;

}